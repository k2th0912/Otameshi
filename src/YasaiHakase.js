import React,{ useState,useEffect,useRef,Component,useContext } from 'react';
import type {Node} from 'react';
import {
  SafeAreaView,
  ScrollView,
  StatusBar,
  StyleSheet,
  Text,
  useColorScheme,
  View,
  TouchableOpacity,
  Button,
} from 'react-native';

const YasaiHakase: () => Node = (props) => {

  return (
    <View>
      <Text>やさい博士のメイン画面</Text>
    </View>
  );
};
export default YasaiHakase;
