import React,{ useState,useEffect,useRef,Component,useContext } from 'react';
import type {Node} from 'react';
import {
  SafeAreaView,
  ScrollView,
  StatusBar,
  StyleSheet,
  Text,
  useColorScheme,
  View,
  TouchableOpacity,
  Button,
} from 'react-native';

const Gakutabe: () => Node = (props) => {

  return (
    <View>
      <Text>ガクタベクイズのメイン画面</Text>
    </View>
  );
};
export default Gakutabe;
